{
  "name": "cineops-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "node scripts/validate.mjs && node --check app.mjs && node --check src/core.mjs && node --check src/scenarios.mjs",
    "test": "node --test",
    "start": "python3 server.py"
  },
  "engines": {
    "node": ">=20"
  }
}
