# infra/

Deployment configuration for the Cloud Run service and supporting resources
(Dockerfile service config, Secret Manager references, Grafana/Prometheus/Loki
provisioning for the local stack).

Status: scaffold. Tracked in:

- #28 — server-side agent orchestrator service on Cloud Run
- #31 — live incident telemetry simulator (docker-compose stack)
- #37 — CI/CD auto-deploy to Cloud Run
